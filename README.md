# Private Macro Desk

Private trading cockpit for two traders sharing the same trading account.

## Development

```bash
npm install
npm run dev
```

Open [http://localhost:3000](http://localhost:3000). The home page redirects to `/dashboard`.

## Checks

```bash
npm run lint
npm run build
```
